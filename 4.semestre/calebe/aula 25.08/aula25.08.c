#include <stdio.h>
v (int numeros[]){
  for (int i =0; i<5; i++) {
    if (numeros[i]== 2) { 
      printf ("encontrou!!");
      }
    }
  }
  
int  main() {
  int numeros[]={1,2,3,4,5];
  buscar(numeros);
  
  return 0;
}